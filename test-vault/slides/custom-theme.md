---
marp: true
theme: my-theme
---

# Custom theme

This deck uses `themes/my-theme.css` (registered via `.marprc.yml`).

Edit the CSS file and the preview should refresh.

---

## Code blocks

```ts
const greeting = 'Hello, Marp';
console.log(greeting);
```

Links keep their colour: [Marp homepage](https://marp.app/).

---

## End

Try switching back to `theme: default` in the frontmatter to compare.
